apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - port: 8003
      targetPort: 8003
